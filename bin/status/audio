#!/bin/bash

read -r vlabel vvol vstate < <(wpctl get-volume @DEFAULT_AUDIO_SINK@)
read -r mlabel mvol mstate < <(wpctl get-volume @DEFAULT_AUDIO_SOURCE@)

[[ "${vstate}" != "[MUTED]" ]] && vicon="󰕾" || vicon="󰖁"
[[ "${mstate}" != "[MUTED]" ]] && micon="󰍬" || micon="󰍭"
[[ -z "${mlabel}" ]] && micon="!"

vvol=$(echo "$vvol * 100" | bc)
printf "${mlabel} %s%s %d%%" $micon $vicon $vvol
